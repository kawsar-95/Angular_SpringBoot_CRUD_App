//Before defining the EmployeeListComponent, let’s define an Employee class for working with employees.
export class Employee {
  id!: number;
  firstName!: string;
  lastName!: string;
  emailId!: string;
}
